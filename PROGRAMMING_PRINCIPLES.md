1. Принцип don`t repeat yourself
Цей метод використовується для того, щоб менше дублювати, і виносити в окремі методи для повторення.

[Program.cs, рядки 191–194](./Program.cs#L191-L194) — метод `PrintAirplane()` використовується повторно для виводу даних про рейси.

2. Принцип kiss 
Простий код без зайвих складностей

[Program.cs, рядки 170–189](./Program.cs#L170-L189) — метод `ReadDate()` реалізує просту перевірку введених даних у циклі.

3.Принцип solid
Кожен клас відноситься до однієї певної сутності

[Program.cs, рядки 4–36](./Program.cs#L4-L36) — клас `Date` відповідає тільки за роботу з датами. 
[Program.cs, рядки 38–72](./Program.cs#L38-L72) — клас `Airplane` відповідає лише за інформацію про рейс.